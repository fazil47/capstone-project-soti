import { ProductCategoryService } from './product-category.service';

describe('ProductCategoryService', () => {
  it('should create an instance', () => {
    expect(new ProductCategoryService()).toBeTruthy();
  });
});
